
export default function InstallPrompt() {
  // Component is now minimal - browser will show native install prompt automatically
  // No custom UI needed, just let the browser handle it
  // The browser's native install prompt will appear automatically when appropriate
  
  // Return null - no custom UI to show
  // Browser will handle the install prompt natively
  return null;
}

