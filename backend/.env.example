# ─────────────────────────────────────────────────────────────────────────────
# Lebrq Backend - Environment Configuration (.env)
#
# This file is for LOCAL DEVELOPMENT ONLY.
# - Git-ignored (.gitignore)
# - NOT required in production (use Cloud Run environment variables instead)
# - Do NOT commit secrets to version control
# ─────────────────────────────────────────────────────────────────────────────

# ─── Environment Mode ───────────────────────────────────────────────────────
ENVIRONMENT=development
DEBUG=False
PORT=8000

# ─── Database Configuration ─────────────────────────────────────────────────

# Option 1: SQLite (Local Development - Default)
# Use this for quick local testing without setting up a database server
DB_URL=sqlite+aiosqlite:///./lebrq.db

# Option 2: PostgreSQL/Supabase (Comment out DB_URL above and use this)
# DATABASE_URL=postgresql+asyncpg://user:password@host:5432/database

# Option 3: MySQL (Comment out DB_URL above and use this)
# DATABASE_URL=mysql+aiomysql://user:password@host:3306/database

# Connection pool tuning (optional - defaults are reasonable)
# DB_POOL_SIZE=5
# DB_MAX_OVERFLOW=5
# DB_POOL_TIMEOUT=30
# DB_POOL_RECYCLE=280

# ─── Security ───────────────────────────────────────────────────────────────

# CRITICAL: Change SECRET_KEY in production!
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=change-me-in-production

# Admin credentials (for initial setup if no admin exists)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# ─── CORS & Frontend ────────────────────────────────────────────────────────

# Comma-separated list of allowed origins (Frontend domains that can call API)
# Local Development:
# CORS_ORIGINS=http://localhost:3000,http://localhost:19006,http://127.0.0.1:19006

# Production Examples:
# CORS_ORIGINS=https://lebrqapp.netlify.app,https://yourdomain.com,https://api.yourdomain.com

# Current Configuration (Default):
# CORS_ORIGINS=http://localhost:19006,http://localhost:3000,http://127.0.0.1:19006,https://lebrqapp.netlify.app

# Frontend URL (for redirects, CTAs in notifications)
FRONTEND_URL=http://localhost:19006

# For Netlify deployment, set to:
# FRONTEND_URL=https://lebrqapp.netlify.app

# ─── Third-Party Service Integration ────────────────────────────────────────

# Google Places API
# GOOGLE_PLACES_API_KEY=your-api-key-here
LOCATION_PROVIDER=nominatim
# USE_MOCK_LOCATION_DATA=False

# Twilio (SMS/WhatsApp)
# TWILIO_ACCOUNT_SID=
# TWILIO_AUTH_TOKEN=
# TWILIO_PHONE_NUMBER=
# TWILIO_WHATSAPP_NUMBER=

# SMTP (Email)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USE_TLS=True
# SMTP_USERNAME=your-email@gmail.com
# SMTP_PASSWORD=your-app-password

# Route Mobile WhatsApp Business API
# ROUTEMOBILE_BASE_URL=https://api.rmlconnect.net
# ROUTEMOBILE_CLIENT_ID=
# ROUTEMOBILE_CLIENT_SECRET=
# ROUTEMOBILE_SENDER=+91xxxxxxxxxx
# ROUTEMOBILE_AUTH_MODE=oauth

# ─── Timezone ────────────────────────────────────────────────────────────────

LOCAL_TIMEZONE=Asia/Kolkata

# ─────────────────────────────────────────────────────────────────────────────
# PRODUCTION DEPLOYMENT (Google Cloud Run)
# ─────────────────────────────────────────────────────────────────────────────
#
# Set these as Cloud Run environment variables (NOT in .env):
#
# 1. DATABASE_URL=postgresql+asyncpg://...
#    - Use Supabase PostgreSQL connection string
#    - Include all credentials securely
#
# 2. SECRET_KEY=<long-random-value>
#    - Generate: python -c "import secrets; print(secrets.token_urlsafe(32))"
#
# 3. ENVIRONMENT=production
#
# 4. ADMIN_PASSWORD=<secure-password>
#
# 5. Other credentials (API keys, tokens) as needed
#
# PORT is set automatically by Cloud Run (8080)
# .env file is NOT read in production (ignored)
#
# ─────────────────────────────────────────────────────────────────────────────
# ROUTEMOBILE_USERNAME=
# ROUTEMOBILE_PASSWORD=
# ROUTEMOBILE_LOGIN_PATH=/oauth/token
# ROUTEMOBILE_MESSAGES_PATH=/v1/messages
# ROUTEMOBILE_AUTH_HEADER_BEARER=true

# Razorpay LIVE credentials (required for payments)
# These are read directly via os.getenv by the payment service
# Do NOT commit real values. Set them in production environment.
# RAZORPAY_LIVE_KEY_ID=rzp_live_XXXXXXXXXXXXXXXX
# RAZORPAY_LIVE_KEY_SECRET=your_live_secret_here
