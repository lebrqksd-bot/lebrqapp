- chore: remove FloatingChat widget and unmount from tabs layout

The temporary floating AI chatbox has been removed per request. If you want it back later, restore `components/FloatingChat.tsx` and mount it in `app/(tabs)/_layout.tsx` under the `<Tabs />` container.